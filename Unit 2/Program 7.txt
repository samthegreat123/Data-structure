#include <stdio.h>

#define MAX 100

int stack[SIZE];
int top = -1;

void push(int value)
{
    stack[++top] = value;
}

int pop()
{
    return stack[top--];
}

int main()
{
    int num, i, divisor;

    printf("Enter a number: ");
    scanf("%d", &num);

    for(i = 2; i <= num; i++)
    {
        if(num % i == 0)
        {
            push(i);
        }
    }

    divisor = stack[0];

    printf("Smallest divisor of %d is %d", num, divisor);

    return 0;
}